services:
  - type: web
    name: pokemon-scraper
    env: python
    buildCommand: pip install -r requirements.txt
    startCommand: python pokemon_scraper.py
    plan: free
    envVars:
      - key: TELEGRAM_TOKEN
        sync: false
      - key: TELEGRAM_CHAT_ID
        sync: false
